# PJH-RPG

Realm of the Mad God 스타일의 웹 브라우저 2D 도트 MMORPG.
탑다운 뷰, 실시간 멀티플레이, 무기별 공격(근접 스윙/원거리 화살), 몬스터, 레벨/인벤토리/아이템/상점 시스템.

**이 프로젝트는 픽셀아트 기반 게임입니다.** 지금은 실제 도트 스프라이트 대신 Phaser Graphics로
찍은 색상 도형 placeholder를 쓰고 있지만(추후 실제 픽셀아트 에셋으로 교체 예정), 렌더링은 이미
`pixelArt: true`(nearest-neighbor, 블러 없는 각진 확대)로 설정해뒀습니다.

## 기술 스택

- **클라이언트**: [Phaser 3](https://phaser.io/) (CDN 로드, 번들러 없음, 순수 ES Module)
- **서버**: Node.js + Express + [Socket.io](https://socket.io/)
- **저장**: 브라우저 `localStorage` (계정/로그인 없음, 기기별로 캐릭터 저장)

## 실행 방법

```
npm install
npm run dev
```

`http://localhost:3000` 을 여러 브라우저 창(또는 탭)으로 열면 서로 다른 플레이어로 접속되어
실시간으로 위치가 동기화됩니다.

## 조작

- 이동: WASD / 방향키
- 조준/공격: 마우스 커서 방향, 클릭으로 공격 (장착 무기가 검이면 근접 스윙, 활이면 화살 발사)
- 화면 확대/축소: 마우스 휠 (1배~3배, 기본 2배로 줌인된 상태로 시작)
- 인벤토리 열기/닫기: **E** (또는 Esc로 닫기), 마인크래프트처럼 모달로 열림
- 핫바 아이템 장착/사용: **숫자키 1~5**
- 상점 열기: 좌상단 HUD의 "상점 열기" 버튼 (**상점 텐트 근처에 있을 때만 활성화**)
- 퀘스트 받기: **퀘스트 NPC를 직접 클릭**하거나 좌상단 HUD의 "퀘스트 받기" 버튼 (둘 다 NPC 근처에
  있을 때만 동작 — 대화창이 열리고 "수락"을 눌러야 실제로 퀘스트가 생김)
- 관리자(베타 디버그) 패널: 우하단 "⚙ 관리자 (베타)" 버튼

## 프로젝트 구조

```
PJH-RPG/
├── server/
│   └── index.js          # 정적 서빙 + Socket.io + 맵/아이템/레벨/인벤토리/상점 authoritative 상태
├── client/
│   ├── index.html         # Phaser/Socket.io CDN 로드 + HUD/핫바/인벤토리·상점 모달/미니맵 DOM
│   └── src/
│       ├── main.js        # Phaser 게임 설정
│       ├── config.js      # 클라이언트 상수 (shared/constants.js와 값 동기화 필요)
│       ├── items.js       # 아이템 카탈로그 (shared/items.js와 동기화 필요)
│       ├── leveling.js    # XP 계산 표시용 (shared/leveling.js와 동기화 필요)
│       ├── storage.js     # localStorage 캐릭터 저장/불러오기
│       ├── network.js     # Socket.io 클라이언트 래퍼
│       ├── ui.js          # HUD / 핫바 / 인벤토리·상점 모달 DOM 렌더링 및 E키 토글
│       ├── minimap.js     # 우상단 미니맵 (섬 지형 축소 렌더 + 플레이어/상점 점)
│       ├── sound.js       # Web Audio API 효과음 (오디오 파일 없음)
│       └── scenes/
│           └── GameScene.js  # 타일맵, 이동, 카메라, 투사체, 아이템 픽업, 멀티플레이 동기화
├── shared/
│   ├── constants.js       # 서버가 require하는 공용 상수 (타일 크기, 속도 등)
│   ├── items.js           # 아이템 카탈로그 + 가격 (서버 authoritative)
│   ├── leveling.js        # XP 공식 + 레벨업 적용 로직 (서버 authoritative)
│   └── islandMap.js       # 시작의 섬 지형 생성기 (서버 전용, 클라이언트는 결과 배열만 받음)
└── package.json
```

## 유저 데이터 구조

캐릭터는 계정 시스템 없이 브라우저 `localStorage`(키: `pjh-rpg-character`)에 저장되고,
접속 시 서버로 전송되어 서버 쪽 `players[socket.id]`에 병합됩니다.

```js
{
  playerId: "uuid",          // 클라이언트가 최초 생성, 기기 내에서 캐릭터 식별용
  level: 1,                  // 1~10 (시작의 섬 레벨 캡)
  xp: 0,                     // 다음 레벨까지 누적 경험치 (레벨업 시 초과분만 이월)
  hp: 20,
  maxHp: 20,                 // 레벨에 따라 자동 계산 (shared/leveling.js maxHpForLevel)
  gold: 0,
  inventory: [               // 고정 12슬롯, 빈 슬롯은 null (앞 5칸은 하단 핫바에도 항상 표시)
    { itemId: "health_potion", qty: 3 },
    null, null, /* ... */
  ],
  equipped: { weapon: "wooden_sword", armor: null },
}
```

서버 쪽 플레이어 상태는 여기에 `x, y, rotation, color`(위치/외형, 저장 대상 아님)가 추가된 형태입니다.

## 맵 설계 — 시작의 섬 (레벨 1~10)

- `shared/islandMap.js`가 160×160 타일(5120×5120px) 섬을 절차적으로 생성합니다. 섬 중심에서의
  거리로 지형을 결정: 내륙은 잔디/흙(의사난수로 흙 patch 흩뿌림), 해안 5타일은 모래, 섬 반지름
  밖은 전부 바다. (이전엔 50×50이라 카메라가 거의 안 움직였는데, 면적 기준 약 10배로 키움)
- 서버가 한 번만 생성해서 클라이언트에 그대로 전송 — 생성 로직은 서버에만 있고, 클라이언트는 받은
  배열을 렌더링/충돌/미니맵에만 사용합니다.
- **바다 진입 제한**: `LEVEL_REQUIRED_FOR_SEA = 10` 기준으로
  - 레벨 10 미만: 바다 타일에 Arcade Physics 충돌이 걸려 물리적으로 진입 불가
  - 레벨 10 이상: 진입은 가능하지만 이동 속도가 `WATER_SPEED_MULTIPLIER(0.5)`로 감소 (배 없이 헤엄치는 느낌)
  - 이 판정은 레벨이 바뀔 때마다(`applyWaterCollision`) 다시 계산됩니다.
- 타일 그래픽은 아직 실제 도트 스프라이트가 아니라 색상 사각형 placeholder입니다
  (잔디=초록, 흙=갈색, 모래=베이지, 바다=파랑) — 도트 스프라이트 전환은 이후 과제로 미룸.
- **풀-흙 경계 블렌딩**: 흙 patch는 4×4 타일 블록 단위로 뭉쳐서 생성되고(`shared/islandMap.js`
  `applyGrassDirtBlend`), 서로 다른 타입과 맞닿은 타일은 `GRASS_EDGE`/`DIRT_EDGE`라는 시각 전용
  타일로 바뀌어 클라이언트에서 반점(디더) 무늬로 렌더링됩니다(`GameScene.drawDitherTile`) — 각진
  사각형 경계 대신 부드럽게 이어지는 것처럼 보이게 하는 용도로, 충돌/이동 판정에는 영향 없음.

## 자연물 오브젝트 (엄폐/무빙)

- 나무·바위·수풀 160개가 섬 전체(상점 반경 4타일 안쪽은 제외)에 흩뿌려져 있고, 정적
  Arcade Physics 바디로 플레이어 이동을 막습니다 — RotMG 특유의 "장애물 사이를 누비는 무빙"을
  위한 엄폐물입니다.
- 화살도 오브젝트에 막힙니다(서버 tick에서 화살 위치가 `OBSTACLE_RADIUS(14px)` 안에 들어오면
  소멸). 근접 공격은 오브젝트를 고려하지 않음(장애물 뒤 몬스터도 사거리 안이면 맞음 — 시야선
  차단은 미구현).
- 한 번 생성되면 사라지지 않는 정적 데이터라 실시간 이벤트 없이 `init` 페이로드에만 담아 보냅니다.

## 캐릭터 / 몬스터 / 상점 외형

- 원 하나짜리 placeholder 대신, 몸통(타원) + 머리(원) + 눈으로 구성된 탑다운 사람 실루엣
  (`GameScene.makeCharacterTexture`). 회전하면 머리가 바라보는 방향이 그대로 보여서 조준 방향을
  파악할 수 있음(원일 때는 회전해도 겉보기 변화가 없었음).
- 몬스터는 종류별로 색만 다른 뿔 달린 실루엣(`makeMonsterTexture`)이라 플레이어와 한눈에 구별됨.
- 몬스터처럼 **내 캐릭터 머리 위에도 HP바**(초록색, 몬스터는 빨간색이라 색으로 아군/적군이
  구분됨)가 항상 떠 있습니다 — HUD 구석 체력 바와 별개로, 캐릭터를 보고 있을 때도 체력을 바로
  확인할 수 있게. 다른 플레이어의 HP는 아직 서버가 브로드캐스트하지 않아서 표시되지 않습니다.
- 플레이어/몬스터 발밑에 옅은 반투명 그림자 타원을 깔아서 공중에 떠 있는 느낌을 없앴습니다.
- 캐릭터·몬스터·나무/바위/수풀은 모두 자신의 y좌표를 depth로 써서(Y-sorting) 화면 아래쪽에
  있을수록 위에 그려집니다 — 나무 뒤로 걸어 들어가면 가려지는 식.
- 상점은 노란 사각형 대신 줄무늬 시장 텐트 모양 스프라이트(`makeShopTexture`)로 바뀌었습니다.
- 아직 색상 도형 placeholder이고, `pixelArt: true`라 확대돼도 흐려지지 않고 각지게 보임.

## 시각 효과(VFX)

- **근접 스윙**: 예전엔 그냥 얇은 삼각형 "칼자국"이라 칼처럼 안 보인다는 지적을 받아서, 손잡이
  (갈색) + 날밑(가드) + 칼날(은색, 중앙 하이라이트 포함)로 구성된 실제 검 모양으로 다시 그림
  (`makeSlashTexture`). 조준 방향(=캐릭터가 바라보는 방향, `faceMouse`와 동일한 각도 계산)을
  기준으로 좌우 40도씩 부채꼴을 그리며 사라짐 — 방향 자체는 원래도 맞았지만, 모양이 너무
  추상적이라 방향이 잘 안 느껴졌던 것.
- **화살 잔상**: 화살이 날아가는 동안 30ms마다 옅고 투명한 잔상을 남겨 궤적이 잘 보이게 함.
- **몬스터 피격 플래시**: 데미지를 받으면 몬스터 스프라이트가 80ms간 흰색으로 번쩍임
  (`setTintFill` → `clearTint`).

## 전투 — 무기별 공격 모션

- 무기 카탈로그의 `attackType`이 공격 방식을 결정합니다: `melee`(검류)는 근접 스윙,
  `ranged`(활)는 화살 발사. 아무 무기도 없으면 맨손 근접 공격(최소 데미지)으로 취급.
- **근접**: 클릭 시 서버가 즉시 판정 — 공격자 위치에서 반경 `MELEE_RANGE(46px)`, 조준 방향 기준
  좌우 45도(`MELEE_ARC`) 안에 있는 몬스터에게 무기 데미지를 입힘. 모든 클라이언트가 칼자국
  스윙 애니메이션(`playMeleeSwing`)을 함께 봄(공격자 본인 화면에서만 보이는 게 아님).
  - **버그 수정**: 왼쪽(조준각이 ±π 근처)으로 휘두르면 반대쪽(오른쪽)으로 거의 한 바퀴 도는
    것처럼 보이는 문제가 있었음. 원인은 Phaser의 `rotation` 트윈에 절대각(`angle+0.7`)을 직접
    넘기면 GameObject의 회전 setter가 값을 (-π, π]로 감싸는 과정에서 의도한 경로와 다르게
    보간되는 것 — 이제는 `rotation`을 트윈 대상 속성으로 아예 안 넘기고, `alpha`/`scale`만
    트윈하면서 `onUpdate`에서 `시작각 + tween.progress * 1.4`로 직접 계산해서 대입함(Phaser의
    각도 보간 로직을 완전히 우회).
- **원거리**: 클릭 시 서버가 화살을 생성하고 발사 이벤트만 브로드캐스트 — 각 클라이언트는 같은
  속도(`ARROW_SPEED`)로 화살을 로컬 시뮬레이션해서 그리고, 서버는 내부적으로(화면에 안 보이는)
  같은 계산을 100ms tick마다 돌려 몬스터 명중 여부를 판정합니다. 맞으면 `arrowRemoved`를 쏴서
  모든 클라이언트에서 화살을 즉시 지우고 데미지를 적용합니다.
  - **버그 수정("터널링")**: 화살이 초당 600px, tick은 100ms라 tick마다 60px씩 건너뛰는데,
    명중 판정 반경(`ARROW_HIT_RADIUS`)은 16px뿐이라 "직전 위치"만 검사하면 그 사이를 화살이
    그냥 통과해버리는 경우가 많았음(멀리서 쏠수록 tick 수가 많아져서 체감상 더 자주 발생).
    지금은 직전 위치 → 현재 위치를 잇는 **선분**과 몬스터 사이의 최단 거리(`distanceToSegment`)
    로 검사해서, tick 사이를 스쳐 지나간 것도 확실히 잡아냄.
- 몬스터가 죽으면 기존 월드 아이템 픽업 시스템을 재사용해 그 자리에 XP 조각(항상)과 골드 더미
  (50% 확률)를 드랍하고, 10초 후 섬 어딘가에 같은 종류로 리스폰됩니다.

## 몬스터 (지역별 스폰 + 어그로 + 종류별로 다른 공격 방식)

- `MONSTER_TYPES`에 3종이 섬 중심에서의 거리(타일 반경)에 따라 나뉘어 스폰됩니다 — 마을에서
  멀어질수록 강해지는 구조:
  - 슬라임 (반경 0~25, HP 15, 접촉 데미지 2, 12마리) — 마을 주변 초보 구간, **근접**만
  - 늑대 (반경 25~45, HP 30, 접촉 데미지 4, 10마리) — 중간 지역, **근접**(슬라임보다 리치가 긴
    `contactRadius=30`이라 더 멀리서도 물어뜯음)
  - 게 (반경 45~섬 끝, HP 45, 접촉 데미지 6, 8마리) — 해안가 외곽, **원거리**(사거리 180px 안에서
    거품 투사체를 던짐 — 붙지 않고 거리를 벌려도 안전하지 않음)
- 서버가 100ms마다 도는 tick(`tickMonsters`)에서 각 몬스터를 스폰 지점 반경 150px 안에서
  2~5초 간격으로 새 목표 지점을 골라 배회시키고, 그 위치를 모든 클라이언트에 주기적으로
  브로드캐스트(`monstersUpdated`)합니다. 클라이언트는 받은 목표 위치로 부드럽게 보간합니다.
- **어그로**: 플레이어가 `AGGRO_RADIUS(110px)` 안에 들어오면 배회 대신 그 플레이어를 실시간으로
  쫓아갑니다. 추격 해제는 더 넓은 `DEAGGRO_RADIUS(220px)`를 기준으로 해서(히스테리시스) 경계선에서
  어그로가 깜빡이며 켜졌다 꺼졌다 하지 않게 함. 공격을 맞으면 때린 사람에게 즉시 어그로가 붙습니다.
- **공격 방식(`attackIfReady`)**: 몬스터별 `attackType`에 따라 분기 —
  - `melee`(슬라임/늑대): `contactRadius` 안까지 붙으면 데미지. 이 접촉 판정 자체가 곧 "때린다"임.
  - `ranged`(게): `rangedRange` 안이면 플레이어 방향으로 투사체를 생성 — 화살과 같은 구조로
    서버가 100ms tick마다 위치를 계산하고(`tickMonsterProjectiles`) 구간 충돌 판정(화살 터널링
    수정과 동일한 `distanceToSegment`)으로 명중을 잡아냄. 클라이언트는 발생 이벤트만 받아 로컬
    시뮬레이션. 장애물에도 막힘.
  - 공격 종류와 무관하게 공용 쿨다운(`MONSTER_ATTACK_COOLDOWN_MS`, 1.4초)을 몬스터별로 따로 추적.
- **공격 시각효과**: 공격이 그냥 조용히 체력만 깎지 않도록, 공격하는 순간(원거리는 명중 여부와
  무관하게 발사하는 순간) 몬스터 쪽에도 `monsterAttack` 이벤트를 브로드캐스트해서 주황색으로 잠깐
  부풀었다 가라앉는 "런지" 연출을 줌(`lungeMonster`) — 맞는 쪽의 빨간 피격 플래시와는 별개로,
  공격 자체가 눈에 보이게 함.

## 플레이어 사망 / 부활

- 플레이어 HP가 0이 되면 `respawnPlayer`가 섬 중앙 근처(마을)로 순간이동시키고 풀피로 회복시킵니다.
  페널티로 **보유 골드의 10%**를 잃습니다. 부활 직후 `PLAYER_RESPAWN_INVULN_MS(1.5초)` 동안은
  무적이라(피격 쿨다운과 별개 체크) 부활하자마자 바로 다시 맞아 죽는 상황을 막아줍니다.
- 클라이언트는 부활 시 캐릭터를 즉시 새 위치로 스냅하고, 무적시간 동안 알파를 깜빡이는 트윈으로
  "지금은 안 맞는다"는 걸 시각적으로 보여줍니다.
- 몬스터에게 맞을 때(`playerHit`)마다 맞은 위치에 빨간 데미지 숫자가 떠오르고, 캐릭터가 빨갛게
  잠깐 번쩍입니다(로컬/다른 플레이어 모두 동일).
- **버그 수정**: 부활하면 나를 죽인 몬스터를 포함해 나를 쫓던 모든 몬스터의 어그로를 명시적으로
  풀어줍니다(`respawnPlayer`에서 `aggroId`를 가진 몬스터를 찾아 초기화). 원래는 거리가
  `DEAGGRO_RADIUS`를 벗어나면 자연히 풀리지만, 마을 근처 몬스터(슬라임 구역이 마을과 겹침)는
  부활 지점과 가까워서 무적시간이 끝나자마자 즉시 다시 쫓아오는 문제가 있었음.

## 퀘스트 (NPC와 대화해서 받는 처치 퀘스트)

- 처음엔 접속하자마자 자동으로 퀘스트가 배정됐었는데, "퀘스트는 사람에게 받게 해달라"는 요청으로
  **퀘스트 담당자 NPC**(로브를 입은 사람, 머리 위에 노란 "!" 표식이 떠 있음)를 상점과는 별도
  위치에 세웠습니다.
- **대화창을 통해 수락**: NPC 근처(`SHOP_INTERACT_RADIUS`, 상점과 같은 반경 재사용)에서 **NPC를
  직접 클릭**하거나 좌상단 HUD의 "퀘스트 받기" 버튼을 누르면 둘 다 같은 대화창을 엽니다
  (`toggleNpcDialogue`). NPC 클릭은 `GameScene.handleAttack`에서 클릭 지점이 NPC의 히트박스
  안에 들어오는지 먼저 검사해서, 근접 무기 스윙/화살 발사보다 우선 처리합니다(NPC를 공격하는 게
  아니라 말을 걸게). 대화창에 퀘스트 없음이면 제안 문구 + "수락" 버튼이, 진행 중이면 진행 상황만
  뜨고 수락 버튼은 숨겨집니다. 실제로 퀘스트가 생기는 건 "수락"을 눌러야만 일어남(`requestQuest`
  이벤트).
- 이미 진행 중인 퀘스트가 있으면 재요청해도 아무 일도 안 일어남(중복 배정 방지). 퀘스트가 없을
  때만 몬스터 종류 하나와 목표 마릿수(4~8마리)를 무작위로 골라 배정합니다(`createQuest`).
- 배정된 종류의 몬스터를 처치(근접이든 화살이든 상관없음, 마지막 타격을 넣은 사람 기준)할 때마다
  진행도가 올라가고, 목표를 채우면 골드(목표수×5)와 XP(목표수×해당 몬스터 XP)를 보상으로 받습니다
  (`questCompleted` 토스트). **완료 후 다음 퀘스트는 자동으로 안 주고**, 다시 NPC에게 가서 받아야
  합니다 — HUD에 "퀘스트: 없음" + "퀘스트 받기" 버튼이 다시 나타남.
- 퀘스트 상태는 서버 메모리에만 있고 localStorage에 저장되지 않습니다 — 새로고침/재접속하면
  진행 중이던 퀘스트는 사라지고, NPC에게 다시 받아야 합니다.

## 레벨 / 아이템 / 인벤토리 / 상점 시스템

- XP 곡선과 레벨업 처리(`applyXp`)는 서버(`shared/leveling.js`)가 authoritative하게 수행합니다.
  레벨업 시 최대 HP가 늘고 풀피로 회복됩니다.
- 아이템 카탈로그(`shared/items.js`): 무기(나무 검/철검/짧은 활), 방어구(가죽 갑옷), 소비 아이템
  (체력 물약, 스택 가능), 각 아이템에 상점 가격(`price`)과 무기는 `attackType`(melee/ranged)이 있음.
- 월드에는 세 종류의 픽업이 흩뿌려져 있고 걸어서 밟으면 자동 습득됩니다(다른 접속자 화면에서도
  즉시 사라짐 — 서버가 단일 진실 소스): 장비/물약(핑크), XP 조각(하늘색), 골드 더미(금색).
  습득한 자리는 8초 후 같은 종류로 리스폰됩니다. 몬스터를 처치해도 같은 방식으로 드랍됩니다.
- **인벤토리(마인크래프트 스타일)**: 하단에 항상 보이는 5슬롯 핫바 + **E키로 여닫는 전체 12슬롯
  모달**(장착 무기/방어구 표시 포함). 슬롯 클릭 또는 **숫자키 1~5**로 핫바 아이템을 바로 장착/사용
  할 수 있습니다(소비 아이템은 사용, 무기/방어구는 장착). 모달이 열려 있는 동안은 이동/공격 입력이
  멈춥니다.
- **아이템 아이콘**: 예전엔 색칠된 네모 안에 아이템 이름 첫 글자만 있어서 뭐가 뭔지 구분이 안
  된다는 지적을 받아서, 종류별로 실루엣이 다른 인라인 SVG 아이콘으로 교체했습니다(근접무기=대각선
  검, 원거리무기=활, 방어구=방패, 소비 아이템=물약병), 색은 그대로 아이템 고유 색을 씁니다
  (`ui.js` `iconMarkup`).
- **상점(근접 제한)**: 섬 중앙 근처 상점 텐트에서 `SHOP_INTERACT_RADIUS(96px,
  약 3타일)` 안에 있어야만 HUD의 "상점 열기" 버튼이 활성화되고 구매/판매가 가능합니다. 서버도
  `buyItem`/`sellItem` 요청마다 플레이어의 실제 좌표로 거리 재검증을 하므로, 클라이언트를 조작해도
  멀리서는 살 수 없습니다. 상점 범위를 벗어나면 열려 있던 모달도 자동으로 닫힙니다.
- **미니맵**: 우상단에 섬 전체 지형을 축소해 표시하고, 내 위치(흰 점) / 다른 플레이어(회색 점) /
  상점(금색 점) / 퀘스트 NPC(보라색 점)를 실시간으로 보여줍니다.

## 효과음 (`client/src/sound.js`)

- 오디오 파일 없이 Web Audio API 오실레이터로 만든 효과음: 검 휘두르기, 화살 발사, 몬스터 타격,
  아이템 습득, 레벨업(4음 아르페지오). 배경음악은 아직 없음(사용자가 "효과음부터"를 선택함 —
  향후 배경음악이나 실제 음원으로 교체하려면 별도로 논의 필요).
- 브라우저 자동재생 정책 때문에 사용자 제스처(첫 클릭/키 입력) 전에는 소리가 안 나는데,
  `GameScene.create()`에서 최초 pointerdown/keydown에 `unlockAudio()`를 걸어둬서 게임 시작 직후
  첫 조작에서 자연스럽게 풀립니다.

## 관리자(베타 디버그) 패널

- 화면 우하단 "⚙ 관리자 (베타)" 버튼으로 골드/레벨을 자유롭게 입력해 즉시 적용할 수 있습니다
  (`adminSetGold`, `adminSetLevel` 이벤트). 레벨을 바꾸면 최대 HP도 재계산되고 풀피로 회복됩니다.
- **인증이 전혀 없습니다.** 지금 단계(베타 테스트)에서 빠르게 확인해보기 위한 용도이며, 정식
  서비스 전에는 반드시 제거하거나 서버 쪽 인증/권한 검사를 추가해야 합니다.

## 실시간 이동

- WASD / 방향키로 자유 이동 (그리드 없는 탑다운), 카메라가 플레이어를 따라감
- 마우스 커서 방향으로 캐릭터가 회전 (이동 방향과 별개로 조준 가능)
- Socket.io로 접속/이동/공격/연결 해제를 모든 클라이언트에 실시간 브로드캐스트
  - 위치 동기화는 클라이언트가 주기적으로(50ms) 전송 → 다른 클라이언트는 보간(lerp)해서 부드럽게 표시

## 알려진 한계 / 다음 단계 (예정)

- 몬스터 AI는 배회/어그로/공격(근접 or 원거리)뿐 (도주, 그룹 연계, 보스 패턴 등은 미구현), 종류도 3종뿐
- 몬스터의 어그로/추격도 장애물의 시야선(line of sight)을 고려하지 않음 — 근접 공격과 마찬가지로
  나무 뒤에 있어도 거리만 가까우면 쫓아오고 때림
- 퀘스트가 처치 한 종류뿐(수집/납품/호위 같은 다른 유형 없음), NPC도 그냥 서 있기만 하고 실제
  대화창/스토리는 없음, 서버 메모리에만 있어 재접속하면 진행도가 리셋됨
- PvP(플레이어끼리 전투) 없음 — 근접/원거리 공격 모두 몬스터만 대상으로 함
- 이동/좌표는 여전히 클라이언트를 대부분 신뢰 (물 진입 제한도 클라이언트 판정이라 우회 가능함).
  단, 공격/피격 판정과 상점 거리 검증은 서버가 authoritative하게 함
- 실제 도트 스프라이트/타일셋 이미지 없음 (색상 도형 placeholder, `pixelArt` 렌더링 모드만 켜둔 상태).
  **실제 에셋 파일을 받으면 적용하기로 함** — 준비되면 이미지/스프라이트 시트를 전달할 것
- 배경음악 없음, 효과음만 있음 (사용자가 이번엔 효과음부터 요청함)
- 다른 플레이어의 HP는 지속적인 HP바로 브로드캐스트되지 않음 — 맞는 순간의 데미지 숫자/피격
  플래시(`playerHit`)는 모두에게 보이지만, 머리 위 상시 HP바는 내 캐릭터에게만 있음
- 배(보트) 아이템처럼 "레벨 10 이상에서 정상 속도로 항해" 하는 수단 없음 — 지금은 그냥 감속만 적용
- 로그인/계정 없음 — 같은 브라우저가 아니면 캐릭터가 이어지지 않음
- 관리자 패널에 인증 없음 (베타 전용, 위 "관리자" 섹션 참고)
